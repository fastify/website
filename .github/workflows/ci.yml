name: CI

on:
  push:
  pull_request:

jobs:
  build_and_deploy:
    name: Linting
    runs-on: ubuntu-latest
    steps:
      - name: Git Checkout
        uses: actions/checkout@v2
      - name: Install Packages
        run: npm install
      - name: Build App
        run: npm run lint
